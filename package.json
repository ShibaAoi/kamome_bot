{
  "name": "kamome_bot",
  "version": "1.0.0",
  "description": "Cloudflare Workersで動くフードコートかもめのDiscord Bot",
  "main": "worker/index.mjs",
  "scripts": {
    "check": "node --check worker/index.mjs && node --check scripts/register-worker-commands.mjs",
    "test": "node --test",
    "worker:dev": "wrangler dev",
    "worker:deploy": "wrangler deploy",
    "worker:check": "wrangler deploy --dry-run --outdir .wrangler-dist",
    "worker:commands": "node scripts/register-worker-commands.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "wrangler": "^4.102.0"
  }
}
